<h2 class="tittle text-center"><%= t ".infomation_suggest" %></h2>
<table class="table table-striped table-bordered table-hover table-condensed">
  <thead>
    <tr>
      <th class="text-center"><%= t ".suggest_name_label" %></th>
      <th class="text-center"><%= t ".suggest_description" %></th>
    </tr>
  </thead>
  <tbody class="text-center">
    <td><%= @suggest_product.product_name %></td>
    <td><%= @suggest_product.product_description %></td>
  </tbody>
</table>
<b><%= link_to t("suggest.back"), new_suggest_product_path %></b>
